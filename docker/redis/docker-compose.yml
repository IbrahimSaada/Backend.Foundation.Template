services:
  redis:
    image: redis:7.4-alpine
    container_name: foundation-template-redis
    ports:
      - "6379:6379"
    command: ["redis-server", "--save", "", "--appendonly", "no"]
    restart: unless-stopped
